import React from "react";

import './../index.css';

const Notes = () => {
    return (
        <section styleName="notes">
            Notes Placeholder Text
        </section>
    );
};

export default Notes;